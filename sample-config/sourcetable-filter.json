{
    "description": "sample configuration for a proxy filter + auth with a remote sourcetable and mountpoints",
    "sources": {
      "*": { "action": "drop" },
      "MOUNTPOINT1": { "user": "user1", "password": "password1", "action": "keep" },
      "MOUNTPOINT2": { "user": "user2", "password": "password2", "action": "keep" }
    }
}
